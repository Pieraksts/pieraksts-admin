/**
 * RETIRED by `SUB-1` — safe to delete.
 *
 * This module drafted a commission contract, which was the launch commercial
 * path before the provider-neutral Business Subscription replaced it. Its
 * server action (`createContract`) is gone and nothing imports this file any
 * more. It is left in place only because this change could not remove the
 * file itself; delete it in the next pass.
 */
export {};
